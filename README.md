# latex-dependency-graph

Takes as input mssql create scripts and creates functional dependency graph for each table.
Graphs are created as latex and can be then visualized in https://www.overleaf.com/
